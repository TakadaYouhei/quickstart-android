general:
  build_dir: .

machine:
  timezone: Asia/Tokyo

dependencies:
    override:
        - echo y | android update sdk --no-ui --filter "API 23, 23.0.1" 

test:
  override:
    - bash build.sh
 
