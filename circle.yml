general:
  build_dir: .

machine:
  timezone: Asia/Tokyo

dependencies:
    override:
        - echo y | android update sdk --no-ui --filter "platform-tools,build-tools-23.0.3" 

test:
  override:
    - bash build.sh
 
